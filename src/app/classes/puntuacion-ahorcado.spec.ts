import { PuntuacionAhorcado } from './puntuacion-ahorcado';

describe('PuntuacionAhorcado', () => {
  it('should create an instance', () => {
    expect(new PuntuacionAhorcado()).toBeTruthy();
  });
});
