import { PuntuacionMayormenor } from './puntuacion-mayormenor';

describe('PuntuacionMayormenor', () => {
  it('should create an instance', () => {
    expect(new PuntuacionMayormenor()).toBeTruthy();
  });
});
